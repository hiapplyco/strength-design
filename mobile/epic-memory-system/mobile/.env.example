# Firebase Configuration (Public keys - safe to expose)
EXPO_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your-auth-domain
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your-storage-bucket
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
EXPO_PUBLIC_FIREBASE_APP_ID=your-app-id

# Third-party APIs (NEVER commit actual keys!)
# These should ideally be moved to backend/Firebase Functions
EXPO_PUBLIC_PERPLEXITY_API_KEY=your-perplexity-api-key-here

# Note: In production, sensitive API keys should be:
# 1. Stored in Firebase Functions configuration
# 2. Called through secure backend endpoints
# 3. Never exposed in client-side code