rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo mode - allow all access for testing
    // WARNING: This is for development/demo only, not for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}