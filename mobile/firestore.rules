rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo mode - allow all access for testing
    // WARNING: This is for development/demo only, not for production
    match /{document=**} {
      allow read, write: if true;
    }
    
    // PRODUCTION RULES (currently disabled for demo):
    // Users can only access their own data
    // match /users/{userId} {
    //   allow read, write: if request.auth != null && request.auth.uid == userId;
    // }
    
    // Workout data - users can only access their own
    // match /workouts/{workoutId} {
    //   allow read, write: if request.auth != null && request.auth.uid == resource.data.userId;
    // }
    
    // Pose analysis data - users can only access their own analysis
    // match /poseAnalyses/{analysisId} {
    //   allow read, write: if request.auth != null && request.auth.uid == resource.data.userId;
    //   allow create: if request.auth != null && request.auth.uid == request.resource.data.userId;
    // }
    
    // Analysis history - users can only access their own history
    // match /analysisHistory/{userId} {
    //   allow read, write: if request.auth != null && request.auth.uid == userId;
    //   match /analyses/{analysisId} {
    //     allow read, write: if request.auth != null && request.auth.uid == userId;
    //   }
    // }
    
    // Chat sessions - users can only access their own
    // match /chatSessions/{sessionId} {
    //   allow read, write: if request.auth != null && request.auth.uid == resource.data.userId;
    // }
    
    // Exercise library - read-only for all authenticated users
    // match /exercises/{exerciseId} {
    //   allow read: if request.auth != null;
    //   allow write: if false; // Only admins can modify exercises
    // }
  }
}